function scrollL() {
    var container = document.querySelector('.shop-content');
    container.scrollLeft -= 250;
  }
  
  function scrollR() {
    var container = document.querySelector('.shop-content');
    container.scrollLeft += 250;
  }